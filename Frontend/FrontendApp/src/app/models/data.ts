export interface LoginData {
    userType: string;
    id: number;
    token: string;
}

export interface RegisterUserData {
}

export interface UserDetailsData {
    id: number;
    userName: string;
    email: string;
    phoneNumber: string;
}